// import { ComponentGenre } from '@types';
// import clsx from 'clsx';
// import React, { forwardRef } from 'react';
// import { useUtilityClasses } from 'utils';
// import './index.scss';
// export interface TemplateProps {
//   classes: string | string[];
// }
// function Template({ classes, ...props }: TemplateProps, forwardedRef?: React.ForwardedRef<any>) {
//   const styledProps = {};

//   const utilityClasses = useUtilityClasses(ComponentGenre.BUTTON, styledProps);

//   return (
//     <div className={clsx(classes, 'peppers-btn', utilityClasses)} ref={forwardedRef} {...props}>
//       Template
//     </div>
//   );
// }

// export default forwardRef(Template);
